# Build 86 原生截图证据采集

Actions #60 的截图检查实际发现 0 张 PNG，故阻止设备 Archive 和 IPA 打包。即使前一个测试步骤返回 success，也不能代替取得原生截图。本次不对缺失原因作无证据的单一归因。

采集改为以脚本所在仓库解析绝对路径的 Python 驱动。使用真实 Release 模拟器应用、官方同步服务和既有在线/离线测试；54个UI场景分别重新启动应用，读取新产生且phase一致的原生JSON，调用simctl截图并检查PNG头、尺寸、大小和SHA-256。先保存原始记录与清单，再清理模拟器，清理后及归档前再次从磁盘独立读取验证。

不生成替代截图，不修改原生运行记录的success，不跳过此前截图门禁。保留逐个命令与文件清单供失败排查。应用业务代码和版本不因采集器调整而更换；Build86最终是否可交付仍取决于实际构建结果与下载后检查。
