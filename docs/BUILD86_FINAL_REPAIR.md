# Build 86 续修核验

## 本轮前置检查失败：Actions #75

提交 `3105a0b8b823321ea1e0b133578f41c5c28b5987` 的运行 `34860482173` 在 `check-upstream190.js:124` 退出，并未进入原生编译。旧断言要求 postinstall 必须等于 `node dependencies-patch.js`，但该提交已增加 `serialize-track-player.cjs` 与 `check-track-player-queue.js`。安装补丁及其8项检查实际通过；旧测试未同步更新是本次明确原因。

本次只修正此契约：按顺序要求三个生产补丁/校验命令，不能缺少、颠倒、用分号继续或用 `|| true` 吞掉失败。没有删除原生播放、同步和截图门槛，没有继续改变UI、解码器、音量或缓存。

提交前定向检查：当前命令通过；六种缺失、调序、跳过和额外命令变体均拒绝；分隔符空白允许；完整脚本语法解析通过。完整仓库和原生检查以新Actions运行结果为准。

## 原生播放修复的独立验证

Actions #74 的线上到缓存切换出现进度调用超时。上一提交将 TrackPlayer 导出方法统一到主队列，并把观察回调里的生命周期进度查询和音效刷新延后，避免重入。增加六轮各自有超时界限的远程/本地缓存切换。这个修复尚需通过原生测试，不能因#75前置检查失败就声称它已经验证失败或通过。

解码器、增益、缓存文件格式及认证实现保持；TrackPlayer的桥接执行队列和观察回调时序确有变化，不能笼统描述为所有音频代码未改。

## 同步失败的既有处理

Actions #73（run `34832416384`）记录：首次认证、初次压缩列表和双向实时变更通过；重连报“列表已移除”，独立进程随后报告 peer rename lost after restart。

生产 storage.ts、listManage.ts、ListEvent 的确定性测试复现了 saveData 删除元数据键后尚未完成 multiSet 的间隙，getUserLists 把空磁盘结果写回活目录。修复为启动一次初始化、并发读取去重、迟到读取版本保护；同步快照与持久化修改共用串行队列并深拷贝。六项回归保留；已删除列表旧请求拒绝和同步方向选择均保留。

## 截图失败的既有处理

应用内场景旋转曾被窗口管理拒绝。独立CI XCTest使用 XCUIDevice.shared.orientation 旋转模拟设备，并核对实际宽高、设备类型、色差以及54份PNG与运行记录。不伪造Dimensions、不旋转输出图片，不使用手机版兼容窗口冒充iPad横屏。

## UI与交付边界

撤回Build84给导航、标题、菜单增加的底板和竖条；保留上游原有输入框、设置分区和多选样式。播放文字检查背景可读性，也检查与普通/辅助文字的色差，中性主题不再回退黑白灰。保留播放标记、定位、来源和规则字重。

本文件由根目录CHANGELOG的Build86条目引用，逐次记录实际失败与修复。任何IPA/Archive交付结论必须对应新Actions成功结果与产物校验，不将源码检查或旧运行记录当成本次原生通过。
