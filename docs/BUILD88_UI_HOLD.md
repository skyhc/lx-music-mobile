# Publication hold — user UI acceptance failed

The user rejected the Build88 run105 UI. Do not publish its draft or binaries.
The correction must put creation in My Lists, configuration in Settings, tasks and
history on the Downloads page, and use the referenced Any Listen CD appearance.
Retain build/test artifact generation. Remove this hold only after the corrected
actual interface and operations have been checked; passing legacy CI alone is
not evidence of visual fidelity. Do not overwrite the old draft assets silently.
