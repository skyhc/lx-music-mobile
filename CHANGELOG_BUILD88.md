# iOS / iPadOS 1.9.0 Build 88

本轮接入：深色底栏主背景、“我的列表”定位当前歌曲、CD／正方形封面、WebDAV目录及音频缓存、默认关闭的下载队列、本机／WebDAV目的地，以及歌单与完整应用数据的加密备份恢复。

下载任务按音源支持获取所选音质，保存前校验音频类型、长度和SHA256；暂停、重试、重启恢复使用验证过的本地前缀和强ETag，服务器不能提供安全续传时重新获取。WebDAV上传采用独立暂存、完整读回校验、禁止覆盖的MOVE；不宣称WebDAV支持通用断点PUT。应用进入后台后暂停下载，重新进入需要手动继续。

完整加密备份包含Documents、Application Support、应用偏好、WebDAV凭据及可选Caches；包括保存到应用内的文件、歌单、用户音源、下载记录和其他持久化软件数据。排除备份归档本身、临时播放副本、操作系统授权／签名和应用外尚未导入的文件。恢复先解密验证及暂存，下次冷启动、在创建React Native桥之前事务替换；首次启动成功后确认，否则后续启动回滚。歌单模式仅恢复歌单与相关WebDAV账户，保留其他设置和用户文件。

新增功能必须通过对应的生产路径、Apple CryptoKit/CommonCrypto、实际WebDAV HTTP、模拟器跨重启恢复、真实音频播放及离线验证。保留原有回归、全历史比较和54张截图。本文是代码更新记录，不是提前声明CI、真机或Release验收成功。
