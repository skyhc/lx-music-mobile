# v1.9.0 Build 86 续修记录

本文件补充根目录 CHANGELOG.md 的 Build 86 条目；已有历次日志与上游日志完整保留。

## 用户反馈对应修改

- 撤回 Build 84 在主导航、底部标签、列表名称和菜单选中项增加的实色底板及竖条。恢复原项目的文字、图标和箭头表示；不清空上游原有的输入框、设置分页和多选背景。表头维持透明。
- 正在播放的文字同时检查对背景的可读性和与普通文字的色差。彩色主题优先保持主题色相，中性主题使用适度有彩色强调，不再回退普通黑白灰。限制色度，不使用荧光式高饱和色。
- 当前歌曲仅用文字色、来源标识和播放标记表达，不附加整行背景、粗体或竖条；自动定位和手动定位保留，多选及键盘焦点独立。

## Actions #74 失败核实

实际运行记录中，原生同步认证、双向变更、重连以及 MP3 网络播放、音量和暂停恢复先通过；随后在 MP3 从远程音频切换到本地缓存时，原生进度读取未返回，报告停留在未完成状态。离线阶段 MP3 可以播放；FLAC 缓存没有在被中断的在线阶段生成，不能据此断言缓存被启动清空。

代码核查发现 TrackPlayer 只有 requiresMainQueueSetup，并未指定导出方法使用的队列；SwiftAudio 的媒体替换/观察位于主队列，新增生命周期通知又在观察回调内同步读取时间并通知主队列观察者。该组合存在跨队列竞态及重入等待风险。此次没有取得有效在线阻塞线程栈，故不将某一锁栈描述成已观测事实。

## 播放调度修正

- 原生 TrackPlayer 导出方法显式使用主队列，与媒体替换、观察和状态读取串行化；不将“主队列初始化”误当成“所有方法都在主队列执行”。
- 生命周期时间快照和通知移到异步主队列块；音效混音刷新离开观察回调后执行，避免回调内重入修改媒体对象。保留原解码、DSP、音量及缓存数据语义。
- 补丁在依赖安装后按固定锚点应用并检查；锚点未知即失败，不悄悄产出未修复版本。
- 原有原生回归不删除，并增加三轮远程/本地缓存往返播放检查。每次替换独立验证原生进度推进，而非只看状态为播放中。
- 测试诊断在未完成的进度调用持续阻塞时提前采样本应用，避免等进程退出后才取栈；诊断工具失败不覆盖原始失败。原生失败继续阻止 IPA/Archive 打包。
- 模拟器的版本和 Build 与设备 Archive 一致。继续核对实际 iPad/iPhone 类型、真实方向及54份原生组件截图。

## 验证边界

源码/补丁回归不等于原生播放通过。原生运行与 Archive 结果以对应 Actions 及下载后的 JSON、截图、元数据和校验文件为准。本记录不预写构建成功，也不声称已远程测试用户的实体 iPad、个人电脑或局域网。

## 共享强调色续修（基于 6de87ff4 / Actions #76）

### 本次定位与改动

检查 #76 的原生截图及上游 v1.9.0（cd37a979）对应组件后确认，当前分支的表头、列表名称、导航选中项已移除额外底板；不再次清空原有输入框、弹出菜单容器、多选或设置按钮背景。

遗留问题在于 iOS 的通用主题适配只提高文字对背景的对比度。即使播放行通过独立的 playingColor 显示有彩色，导航图标、底部标签、当前列表名称及菜单选中项仍使用可能接近普通黑白灰的 c-primary-font 系列。

- 新增 readableThemeWithAccent，并接入实际 useTheme：仅调整三个通用强调文字色，保留主题原始色相种子、普通文字、布局和全部背景。中性主题使用有彩色强调；不会将全部彩色主题统一改成蓝色。
- 菜单选中及键盘焦点文字按弹出菜单实际底色计算，避免直接沿用页面颜色在紫色/深色菜单上失去足够对比度；不增加选中背景、粗体或竖条。Android 分支保持原配色路径。
- 原生截图用例显式显示一个菜单选中项，并使用与生产 hook 相同的主题转换；报告新增导航/标题三个强调角色及菜单色差指标，任一失败仍阻止通过。
- 在现有 Build86 CI 回归入口增加共享强调色测试，覆盖 16 套内置主题、实际 hook 缓存/换肤、真实导航/列表名称/菜单组件返回的渲染属性、上游箭头及透明背景。旧黑色/灰色强调色作为反例必须被新检查拒绝。

### 已执行的本地验证

- Build86 原有 15 组回归及新增 8 组共享强调色检查通过；原有播放行检查同步使用本次实际主题转换。
- Build82：15 组；Build81：14 组；iPad 播放器布局：19 项；iPad 窗口布局：10 项；目录持久化/同步竞态：6 项，均通过。
- 新工具及其纯 TypeScript 依赖通过 strict 检查；4 个修改/新增 TS/TSX 文件通过语法转译检查；git diff --check 通过。
- 本地容器没有完整 React Native 依赖、Mac/Xcode，也没有真实设备。Build85 检查因缺少 spark-md5 未能启动；未将其记为通过。全项目类型检查、原生运行和 Archive/IPA 仍由原有 Actions 执行，未关闭或绕过。

### #76 既有失败与验证边界

下载的 playback-online.json 显示，iOS 初次认证与 24 首歌曲的初始同步已通过，但“独立桌面协议客户端接收 iOS 初始音乐库”步骤超过 20000ms；音频探针尚未运行。offline 报告随后的“peer rename lost after restart”发生在在线 peer 重命名阶段尚未完成的前提下，不能据此断言本轮出现持久化丢失。

这些记录只描述旧提交 6de87ff4 的 #76，不代表本次提交的原生测试结果。本次不猜测同步超时根因、不改认证/播放实现、不延长或删除失败门槛；也不宣称已生成新 IPA。新提交的构建、截图与设备验证应按其自己的 Actions SHA 核对。
