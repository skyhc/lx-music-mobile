diff --git a/scripts/check-build82-regressions.js b/scripts/check-build82-regressions.js
index c72bec0..4430299 100644
--- a/scripts/check-build82-regressions.js
+++ b/scripts/check-build82-regressions.js
@@ -193,7 +193,7 @@ check('release simulator validates scene style, live status colors and productio
   assert.ok(source('src/tests/playbackSmoke.tsx').includes('scene-based window and minimal native window controls'))
   const script = source('scripts/run-ios-playback-smoke.sh')
   for (const phase of ['darktable','darkmenu','darklibrary','themeswitch','lightagain']) assert.ok(script.includes(phase))
-  assert.ok(script.includes('42 production-view screenshots'))
+  assert.ok(script.includes('54 production-view screenshots'))
 })
 check('Build82 version and seven-fix changelog ship with regression gates', () => {
   assert.ok(JSON.parse(source('package.json')).versionCode >= 82)
diff --git a/scripts/check-build84-regressions.js b/scripts/check-build84-regressions.js
index bacd87c..e2cbd78 100644
--- a/scripts/check-build84-regressions.js
+++ b/scripts/check-build84-regressions.js
@@ -10,9 +10,8 @@ const plain=x=>JSON.parse(JSON.stringify(x));const sleep=ms=>new Promise(r=>setT
 let count=0;const check=async(name,fn)=>{await fn();count++;console.log('PASS '+name)}
 ;(async()=>{
  const {selectionColors}=load('src/utils/selectionColors.ts')
- const {contrastRatio}=load('src/utils/readability.ts')
- await check('selected content remains readable with monochrome light/dark themes',()=>{
-  for(const isDark of [false,true]){const c=selectionColors({isDark});assert.ok(contrastRatio(c.text,c.background)>=7);assert.ok(contrastRatio(c.indicator,c.background)>=3);assert.notEqual(c.background,isDark?'#141414':'#FFFFFF')}
+ await check('multi-selection preserves upstream theme surface; no hard-coded heading selection palette',()=>{
+  for(const isDark of [false,true]){const theme={isDark,'c-primary-background-hover':isDark?'rgba(20,100,80,0.3)':'rgba(70,150,110,0.2)','c-font':isDark?'#ddd':'#333','c-primary-font':'#457865','c-border-background':'#678'};const c=selectionColors(theme);assert.equal(c.background,theme['c-primary-background-hover']);assert.equal(c.text,theme['c-font']);assert.equal(c.indicator,theme['c-primary-font'])}
  })
  const {KeyboardRouter,nextKeyboardIndex}=load('src/core/keyboardRouter.ts')
  await check('keyboard routing isolates modal, visible page and newer focused target',()=>{
diff --git a/scripts/check-build85-regressions.js b/scripts/check-build85-regressions.js
index 38fd6e1..a603b12 100644
--- a/scripts/check-build85-regressions.js
+++ b/scripts/check-build85-regressions.js
@@ -52,19 +52,18 @@ const check = async(name, fn) => { await fn(); checks++; console.log('PASS ' + n
       const c = colors.playingColor(t), rgb = c.match(/\d+/g).map(Number).map(v => v / 255)
       const l = (Math.max(...rgb) + Math.min(...rgb)) / 2
       const s = (Math.max(...rgb) - Math.min(...rgb)) / (1 - Math.abs(2 * l - 1))
-      assert.ok(s <= .66, 'neon saturation'); assert.ok(readability.contrastRatio(c, colors.songSurface(t)) >= 4.8)
+      assert.ok(s <= .74, 'excessive HSL saturation'); assert.ok(colors.playingColorChroma(c, colors.songSurface(t)) <= .181, 'neon chroma'); assert.ok(readability.contrastRatio(c, colors.songSurface(t)) >= 4.8)
       results.push(c)
     }
     assert.equal(new Set(results).size, 6)
   })
-  await check('neutral themes emphasize rather than dim playing text when contrast headroom exists', () => {
+  await check('neutral themes use a distinguishable chromatic foreground, not brighter black/white', () => {
     for (const [bg, normal] of [['#151515', '#dbdbdb'], ['#ffffff', '#444444']]) {
       const t = { isDark: bg !== '#ffffff', 'c-primary': '#888888', 'c-font': normal, 'c-content-background': bg }
       const c = colors.playingColor(t)
-      assert.ok(readability.contrastRatio(c, bg) > readability.contrastRatio(normal, bg) + 1)
-      const rgb = c.match(/\d+/g).map(Number)
-      assert.equal(rgb[0], rgb[1]); assert.equal(rgb[1], rgb[2])
-      assert.ok(rgb[0] > 0 && rgb[0] < 255, 'Do not force absolute black or white')
+      assert.ok(readability.contrastRatio(c, bg) >= 4.8)
+      assert.ok(colors.playingColorChroma(c, bg) >= .085)
+      assert.ok(colors.playingColorDistance(c, normal, bg) >= .16)
     }
   })
   await check('alpha backgrounds and theme changes recalculate the playing foreground', () => {
@@ -207,7 +206,7 @@ const check = async(name, fn) => { await fn(); checks++; console.log('PASS ' + n
     const pkg = JSON.parse(read('package.json')), lock = JSON.parse(read('package-lock.json'))
     const versions = Object.entries(lock.packages).filter(([k]) => k.endsWith('/react-native-quick-base64')).map(([, v]) => v.version)
     assert.deepEqual(versions, ['2.2.2']); assert.equal(pkg.dependencies.buffer, '5.7.1'); assert.equal(pkg.dependencies['spark-md5'], '3.0.2')
-    assert.equal(pkg.versionCode, 85)
+    assert.equal(pkg.versionCode, 86)
   })
   console.log(`${checks} Build85 checks passed (${process.argv.includes('--node-fixture-codecs') ? 'Node fixture codecs; bundled packages still require CI' : 'installed production JS codec packages'}). Native bridge/official-server tests run separately.`)
 })().catch(e => { console.error(e); process.exitCode = 1 })
